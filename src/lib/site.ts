// Canonical production origin, used for metadata, canonical URLs, robots,
// sitemap, and Open Graph. Override per-environment via NEXT_PUBLIC_SITE_URL.
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? 'https://rahuls.dev'
